spring.application.name=learnandassessportal
server.port=8081
# Database Configuration
# NOTE: The database name is now 'learning_portal'
spring.datasource.url=jdbc:mysql://localhost:3306/learning_portal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Your credentials
spring.datasource.username=root
spring.datasource.password=Streak@1910

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File Storage Path (for Video/PDFs as per SRS [cite: 83])
app.file.storage-dir=C:/lap_uploads/

